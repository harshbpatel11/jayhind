{
  "_": "Shared behavioural table for the cost dimension's six refusals — see scripts/check-mirrors.js check 13. `jayhind-client-back/src/const/cost-allocation.const.ts` (the centre tree) and `…/cost-centre-class.const.ts` (the percentage template) enforce them; `jayhindi-client-front/src/utils/cost-rules.util.ts` states them on the Cost Centres masters screen so an operator reads the reason instead of meeting a 400. Both must answer every expanded row with the SAME SENTENCE, and both must match the sentence stated here — so a wording one side quietly improved, and a rule both sides forgot together, are different failures.",

  "__regions": "A case's `given` holds ARRAYS: every combination is expanded and compared, so a case is a statement about a REGION of the fact space rather than one row. The regions of each product rule partition it exactly, and they are split along the SINGULAR/PLURAL seam because these sentences count things — `1 cost centre` and `3 cost centres` are two templates, and a table that dodged the plural (`centre(s)`) would stop comparing the wording that reaches the screen.",

  "__literal": "`classLines` and `classDelete` are declared `literal` in check 13: their fact space is a LIST of lines plus two lookup maps, which is not a product of scalars, and expanding an array of lines would silently turn one multi-line case into N single-line ones — the opposite of what every total-and-duplicate vector is about.",

  "__unknown": "`allocationCount` may be `null`, meaning ABSENT — the browser cannot know whether a centre has been allocated to, and inventing a guess would produce a refusal that is sometimes wrong (P3d‑2's `hasPostings` ruling). Both sides must then answer `null` (allowed) and let the server, which does know, refuse with this same sentence. That equivalence is a rule, not an accident of two signatures, so it is in the table.",

  "messages": {
    "centre-parent-category": "\"${centreName}\" cannot sit under \"${parentName}\": a cost centre and its parent must belong to the same cost category, because every cost report rolls a subtree up within one category.",
    "centre-self-parent": "\"${centreName}\" cannot be its own parent.",
    "centre-cycle": "\"${centreName}\" cannot be moved under \"${parentName}\": that is inside its own subtree, which would turn the tree into a ring and a cost roll-up into a walk that never terminates.",

    "category-primary": "\"${name}\" is the primary cost category and cannot be deleted.",
    "category-centres-1": "\"${name}\" still holds 1 cost centre. Delete or re-file them first.",
    "category-centres-n": "\"${name}\" still holds ${centreCount} cost centres. Delete or re-file them first.",

    "centre-children-1": "\"${name}\" still has 1 cost centre under it. Delete or re-file them first.",
    "centre-children-n": "\"${name}\" still has ${childCount} cost centres under it. Delete or re-file them first.",
    "centre-class-1": "\"${name}\" is part of 1 cost centre class. Remove it from that class first.",
    "centre-class-n": "\"${name}\" is part of ${classLineCount} cost centre classes. Remove it from those classes first.",
    "centre-alloc-1": "\"${name}\" has 1 allocation against it and is part of the books. It can be archived, not erased.",
    "centre-alloc-n": "\"${name}\" has ${allocationCount} allocations against it and is part of the books. It can be archived, not erased.",

    "class-empty": "\"${className}\" allocates to no cost centre. A cost centre class is a split, so it needs at least one.",
    "class-unknown-centre": "Cost centre #${badId} is not one of this company's.",
    "class-unknown-category": "Cost category #${badId} is not one of this company's.",
    "class-wrong-category": "\"${centreName}\" is not a cost centre of \"${categoryName}\" — a class line names the category its own centre belongs to.",
    "class-bad-share": "\"${centreName}\" takes ${share}% of the figure. A class line is a share between 0 and 100%.",
    "class-duplicate": "\"${className}\" names \"${centreName}\" twice under \"${categoryName}\".",
    "class-total": "\"${className}\" allocates ${sum}% of a figure across \"${categoryName}\". A class states a whole split, so each category's lines total 100%."
  },

  "centrePlacement": [
    {
      "id": "place-no-parent",
      "given": { "centreName": ["Sales", "North"], "centreCategoryId": [1, 2], "parentKind": ["none"], "parentName": ["Head Office"] },
      "expect": null,
      "why": "a root centre has no parent to disagree with — the primary category's own top level, which is where every tree starts"
    },
    {
      "id": "place-same-category",
      "given": { "centreName": ["Sales", "North"], "centreCategoryId": [1, 2], "parentKind": ["same"], "parentName": ["Head Office", "Plant"] },
      "expect": null,
      "why": "the ordinary case: a department under a department"
    },
    {
      "id": "place-across-categories",
      "given": { "centreName": ["Sales", "North"], "centreCategoryId": [1, 2], "parentKind": ["other"], "parentName": ["Head Office", "Plant"] },
      "expect": "centre-parent-category",
      "why": "a location under a department would be collected by a Department Summary that has no business seeing it, and the two categories' totals would overlap — the parallel-category invariant is only true if the trees are parallel too"
    }
  ],

  "centreMove": [
    {
      "id": "move-to-root",
      "given": { "centreName": ["Sales"], "centreCategoryId": [1], "parentKind": ["none"], "parentName": ["Head Office"] },
      "expect": null,
      "why": "moving a centre to the top of its own category is always allowed — a move re-signs no figure, because a centre's path carries no denormalised nature the way `acc_groups` does"
    },
    {
      "id": "move-within-category",
      "given": { "centreName": ["Sales"], "centreCategoryId": [1, 2], "parentKind": ["same"], "parentName": ["Head Office", "Plant"] },
      "expect": null,
      "why": "re-organising departments between years is what a business does, and a posted centre is deliberately still movable"
    },
    {
      "id": "move-onto-self",
      "given": { "centreName": ["Sales", "North"], "centreCategoryId": [1, 2], "parentKind": ["self"], "parentName": ["ignored"] },
      "expect": "centre-self-parent",
      "why": "asked FIRST, and separately from the subtree check: a centre's own path is a prefix of itself, so folding the two would answer the ring sentence about a row that is simply pointing at itself"
    },
    {
      "id": "move-into-own-subtree",
      "given": { "centreName": ["Sales"], "centreCategoryId": [1, 2], "parentKind": ["descendant"], "parentName": ["Sales · East", "Sales · West"] },
      "expect": "centre-cycle",
      "why": "checked against the PATHS the reports roll up by, not a parent walk, so the screen and the roll-up cannot disagree about what \"inside\" means"
    },
    {
      "id": "move-into-subtree-across-categories",
      "given": { "centreName": ["Sales"], "centreCategoryId": [1], "parentKind": ["descendant-other-category"], "parentName": ["Sales · East"] },
      "expect": "centre-cycle",
      "why": "ORDER: the subtree check runs before the placement one, so a destination that is both a descendant and in another category answers about the ring. A construction the tree cannot actually hold — which is the point of asserting the order rather than the reachable cases"
    },
    {
      "id": "move-across-categories",
      "given": { "centreName": ["Sales"], "centreCategoryId": [1, 2], "parentKind": ["other"], "parentName": ["Head Office"] },
      "expect": "centre-parent-category",
      "why": "a move delegates its third arm to the placement rule, so the two doors — create and move — answer identically about the same destination"
    }
  ],

  "categoryDelete": [
    {
      "id": "cat-primary",
      "given": { "name": ["Primary Cost Category"], "isPrimary": [true], "centreCount": [0, 1, 4] },
      "expect": "category-primary",
      "why": "asked first: Tally's own category is structural, and F8's lesson (`trx_natures` shipped four structural rows as an editable master) applied before it can be repeated"
    },
    {
      "id": "cat-holds-one",
      "given": { "name": ["Department", "Location"], "isPrimary": [false], "centreCount": [1] },
      "expect": "category-centres-1",
      "why": "the singular is its own template — a category deleted out from under its centres leaves allocations naming a partition that no longer exists, and every per-category Σ becomes unanswerable rather than wrong"
    },
    {
      "id": "cat-holds-many",
      "given": { "name": ["Department", "Location"], "isPrimary": [false], "centreCount": [2, 9] },
      "expect": "category-centres-n",
      "why": "the plural, counted off the tree the screen has already loaded — which is why this arm needs no new payload"
    },
    {
      "id": "cat-empty",
      "given": { "name": ["Department"], "isPrimary": [false], "centreCount": [0] },
      "expect": null,
      "why": "an empty non-primary category is an ordinary master row"
    }
  ],

  "centreDelete": [
    {
      "id": "centre-one-child",
      "given": { "name": ["Sales"], "childCount": [1], "classLineCount": [0, 1, 3], "allocationCount": [null, 0, 1, 4] },
      "expect": "centre-children-1",
      "why": "children are asked FIRST because they are the cheapest to act on, and the arm holds whatever the other two say"
    },
    {
      "id": "centre-many-children",
      "given": { "name": ["Sales", "Plant"], "childCount": [2, 6], "classLineCount": [0, 2], "allocationCount": [null, 0, 3] },
      "expect": "centre-children-n",
      "why": "the plural template"
    },
    {
      "id": "centre-one-class",
      "given": { "name": ["Sales"], "childCount": [0], "classLineCount": [1], "allocationCount": [null, 0, 1, 4] },
      "expect": "centre-class-1",
      "why": "a class line is REFUSED rather than archived because it is fixable in one edit — deleting the centre out from under it would leave a template silently allocating 60% of every figure to nothing"
    },
    {
      "id": "centre-many-classes",
      "given": { "name": ["Sales", "North"], "childCount": [0], "classLineCount": [2, 5], "allocationCount": [null, 0, 2] },
      "expect": "centre-class-n",
      "why": "the plural, and it is DISTINCT classes — counted client-side off `GET /cost-centre-classes/list`, which returns every class with its split, so this arm is exact rather than a guess"
    },
    {
      "id": "centre-one-allocation",
      "given": { "name": ["Sales"], "childCount": [0], "classLineCount": [0], "allocationCount": [1] },
      "expect": "centre-alloc-1",
      "why": "a centre that has been allocated to is history — §4.9 rule 2 one dimension across, so it archives rather than erasing"
    },
    {
      "id": "centre-many-allocations",
      "given": { "name": ["Sales", "North"], "childCount": [0], "classLineCount": [0], "allocationCount": [2, 40] },
      "expect": "centre-alloc-n",
      "why": "the plural"
    },
    {
      "id": "centre-clear-or-unknown",
      "given": { "name": ["Sales"], "childCount": [0], "classLineCount": [0], "allocationCount": [null, 0] },
      "expect": null,
      "why": "THE `null` ROW IS THE RULE: absent means \"not known here\", so both sides allow it, the request goes, and the server — which does know — refuses with the sentence above. A client-side guess would be a refusal that is sometimes wrong, which teaches an operator to ignore refusals"
    }
  ],

  "classLines": [
    {
      "id": "class-no-lines",
      "given": {
        "className": "Standard Split",
        "lines": [],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-empty",
      "why": "a class with no lines is not a split; the DTO refuses it too (`@ArrayNotEmpty`), and this is the sentence the screen shows while somebody is still building one"
    },
    {
      "id": "class-stranger-centre",
      "given": {
        "className": "Standard Split",
        "badId": 99,
        "lines": [{ "categoryId": 1, "costCentreId": 99, "percentage": 100 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-unknown-centre",
      "why": "ownership is a database question asked at the service seam (§4.3 rule 7); what reaches the rule is what the seam loaded, so a stranger's id is simply ABSENT and this arm speaks for it. On the screen the same absence means a centre the operator cannot have picked"
    },
    {
      "id": "class-stranger-category",
      "given": {
        "className": "Standard Split",
        "badId": 77,
        "lines": [{ "categoryId": 77, "costCentreId": 1, "percentage": 100 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-unknown-category",
      "why": "the centre resolves and its category does not — asked in that order, because a line naming a centre nobody has is the more likely typo"
    },
    {
      "id": "class-line-names-wrong-category",
      "given": {
        "className": "Standard Split",
        "centreName": "Sales",
        "categoryName": "Location",
        "lines": [{ "categoryId": 2, "costCentreId": 1, "percentage": 100 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }, { "id": 2, "name": "Location" }]
      },
      "expect": "class-wrong-category",
      "why": "a class line names the category its own centre belongs to — otherwise the per-category totals below would be computed over a partition the centre is not in, and the 100% check would be true of nothing"
    },
    {
      "id": "class-zero-share",
      "given": {
        "className": "Standard Split",
        "centreName": "Sales",
        "share": "0",
        "lines": [{ "categoryId": 1, "costCentreId": 1, "percentage": 0 }, { "categoryId": 1, "costCentreId": 2, "percentage": 100 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }, { "id": 2, "name": "Admin", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-bad-share",
      "why": "a 0% line is a row that will never produce an allocation — `describeAllocationPayloadBlock` refuses a non-positive share on the voucher, so a class that stored one would make its own voucher unsavable"
    },
    {
      "id": "class-over-100-share",
      "given": {
        "className": "Standard Split",
        "centreName": "Sales",
        "share": "140",
        "lines": [{ "categoryId": 1, "costCentreId": 1, "percentage": 140 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-bad-share",
      "why": "ORDER: the per-line bound is checked before the per-category total, so a single 140% line names itself rather than reporting a 140% category — the sentence an operator can act on"
    },
    {
      "id": "class-same-centre-twice",
      "given": {
        "className": "Standard Split",
        "centreName": "Sales",
        "categoryName": "Department",
        "lines": [{ "categoryId": 1, "costCentreId": 1, "percentage": 60 }, { "categoryId": 1, "costCentreId": 1, "percentage": 40 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-duplicate",
      "why": "60 + 40 totals correctly and is still wrong: in a DEFINITION it is somebody having typed the same department twice, with the second row silently overwriting what they meant by the first. Asked BEFORE the total, or this case would pass"
    },
    {
      "id": "class-under-100",
      "given": {
        "className": "Standard Split",
        "categoryName": "Department",
        "sum": "90",
        "lines": [{ "categoryId": 1, "costCentreId": 1, "percentage": 60 }, { "categoryId": 1, "costCentreId": 2, "percentage": 30 }],
        "centres": [{ "id": 1, "name": "Sales", "categoryId": 1 }, { "id": 2, "name": "Admin", "categoryId": 1 }],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": "class-total",
      "why": "the phase's central rule: a definition that totals 90% can never produce a complete allocation, so it would warn on every voucher it is ever used on at a screen with no way to say why. Refused on the master, where a voucher's own partial allocation only warns (§3.7)"
    },
    {
      "id": "class-two-categories-total-100-each",
      "given": {
        "className": "Standard Split",
        "lines": [
          { "categoryId": 1, "costCentreId": 1, "percentage": 60 },
          { "categoryId": 1, "costCentreId": 2, "percentage": 40 },
          { "categoryId": 2, "costCentreId": 3, "percentage": 70 },
          { "categoryId": 2, "costCentreId": 4, "percentage": 30 }
        ],
        "centres": [
          { "id": 1, "name": "Sales", "categoryId": 1 }, { "id": 2, "name": "Admin", "categoryId": 1 },
          { "id": 3, "name": "North", "categoryId": 2 }, { "id": 4, "name": "South", "categoryId": 2 }
        ],
        "categories": [{ "id": 1, "name": "Department" }, { "id": 2, "name": "Location" }]
      },
      "expect": null,
      "why": "PER CATEGORY, not per class: this class totals 200 and is correct, because Department and Location cut the same figure in parallel. Demanding 100 of the whole class is what would make every two-category class unstorable"
    },
    {
      "id": "class-two-categories-one-short",
      "given": {
        "className": "Standard Split",
        "categoryName": "Location",
        "sum": "70",
        "lines": [
          { "categoryId": 1, "costCentreId": 1, "percentage": 60 },
          { "categoryId": 1, "costCentreId": 2, "percentage": 40 },
          { "categoryId": 2, "costCentreId": 3, "percentage": 70 }
        ],
        "centres": [
          { "id": 1, "name": "Sales", "categoryId": 1 }, { "id": 2, "name": "Admin", "categoryId": 1 },
          { "id": 3, "name": "North", "categoryId": 2 }
        ],
        "categories": [{ "id": 1, "name": "Department" }, { "id": 2, "name": "Location" }]
      },
      "expect": "class-total",
      "why": "the complete category is passed over and the incomplete one is NAMED — a message saying only \"the totals are wrong\" would send somebody to check the half that is right"
    },
    {
      "id": "class-thirds",
      "given": {
        "className": "Standard Split",
        "lines": [
          { "categoryId": 1, "costCentreId": 1, "percentage": 33.3333 },
          { "categoryId": 1, "costCentreId": 2, "percentage": 33.3333 },
          { "categoryId": 1, "costCentreId": 3, "percentage": 33.3334 }
        ],
        "centres": [
          { "id": 1, "name": "Sales", "categoryId": 1 }, { "id": 2, "name": "Admin", "categoryId": 1 },
          { "id": 3, "name": "Works", "categoryId": 1 }
        ],
        "categories": [{ "id": 1, "name": "Department" }]
      },
      "expect": null,
      "why": "four decimals is the column's own precision, so a third split three ways IS storable — the total is compared with half a unit of the last place rather than with `===`, and the rupee rounding is the expansion's job rather than this rule's"
    }
  ],

  "classDelete": [
    {
      "id": "class-always-deletable",
      "given": {},
      "expect": null,
      "why": "THE ABSENCE OF A GUARD IS THE DECISION. A class is a stencil: the shares it produced were copied onto the voucher and the class id was deliberately not stored, so nothing points at it and deleting one changes no figure. Mirrored so the SCREEN cannot grow a refusal the server does not make — the opposite direction from every other row in this table"
    }
  ]
}
